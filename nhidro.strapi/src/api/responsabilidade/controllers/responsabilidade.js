'use strict';

/**
 *  responsabilidade controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::responsabilidade.responsabilidade');
