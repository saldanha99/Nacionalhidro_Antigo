'use strict';

/**
 * responsabilidade service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::responsabilidade.responsabilidade');
