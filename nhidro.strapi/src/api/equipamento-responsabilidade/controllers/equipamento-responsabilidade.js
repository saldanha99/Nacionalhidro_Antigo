'use strict';

/**
 *  equipamento-responsabilidade controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::equipamento-responsabilidade.equipamento-responsabilidade');
