'use strict';

/**
 * escala-veiculo service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::escala-veiculo.escala-veiculo');
