// const relatorio = require("../src/services/faturamento/index");
// const { Enum_StatusFaturamento } = require("../utils/enums");

module.exports = {
    //   '0 0 12 1/1 * ? *': async ({ strapi }) => {
    //     console.log(new Date());
    //     const listaFaturamentos = await strapi.services["api::faturamento.faturamento"].getFaturamentosByStatus(Enum_StatusFaturamento.EmAprovacao);
    //     console.log(listaFaturamentos);
        // for(let item of listaFaturamentos) {
        //   item = strapi.services["api::ordem-servico.ordem-servico"].calculaDemonstrativos(item);
        //   relatorio.gerarRelatorioCobranca(item);
        // }
    //   },
    };